# SF 탐사단 제품 방향 및 지속 개선 로드맵

> 이 문서는 SF 탐사단의 제품 원칙, Production 기준선, 기술적 경계, 개선 우선순위를 지속적으로 관리하는 기준 문서다. 기능을 추가하거나 IA·데이터 계약·공개 범위를 변경할 때 이 문서를 함께 갱신한다.

- 최초 기준선: 2026-08-05 HomeV2 Production 출시
- 기준 커밋: `502787d6a92a0692696ac7ff3301d1eaf1d37d61`
- Production: <https://www.sf-explorer.net>
- 출시 판정: P0 0 / P1 0

## 1. 제품 명제

SF 탐사단은 기능이 많은 SF 포털이 아니라 다음 순환을 돕는 **조용한 SF 관측소**다.

> SF를 발견한다 → 감상을 기록한다 → 다른 탐사자의 공개 신호와 연결된다 → 다시 새로운 SF를 발견한다.

Home은 모든 기능을 축소해 모아 놓는 메가 대시보드가 아니라 이 순환에 진입하는 가벼운 허브로 유지한다.

## 2. 제품 언어

- **탐사**: 작품·관측 정보·SF 개념을 둘러보는 상위 공간
- **발견**: 탐사 과정에서 사용자가 수행하는 행동
- **기록**: 개인 감상을 작성하고 보존하는 행동
- **신호**: 사용자가 명시적으로 공개한 기록
- **네트워크**: 공개 신호가 작품·개념·다른 기록과 연결되는 공간

상위 내비게이션은 `탐사 / 기록 / 네트워크 / 내 정보`를 기준으로 유지한다. 동일 목적지에 탐색·발견·아카이브 등 서로 다른 이름을 혼용하지 않는다.

## 3. 보존할 핵심 원칙

### Home

- 단일 제품 흐름인 `발견 → 기록 → 연결`을 유지한다.
- 주 행동은 한 번에 하나가 명확해야 한다.
- Home에 전체 검색·작성 폼·지도·설정·프로필 기능을 다시 합치지 않는다.
- 전역 읽기 모드는 Home에서 렌더링하지 않는다.
- 섹션 순서는 `탐사 순환 → 오늘의 발견 → 새로 포착된 SF → 최근 탐사 신호`를 기준으로 한다.

### 데이터와 상태

- 레이더·통계·네트워크에는 실제 데이터만 사용한다.
- 가짜 신호, 랜덤 활동량, 데모 네트워크를 실제 활동처럼 표시하지 않는다.
- 정상 0건, 로딩, 설정 누락, 네트워크 오류, 부분 소스 장애를 구분한다.
- 부분 실패를 전체 성공이나 전체 갱신으로 표현하지 않는다.
- stale fallback을 사용하면 운영 상태에서도 명시적으로 구분한다.

### 기록과 공개

- 기록 작성과 공개는 별개의 선택이다.
- 로그인 전 기록은 이 기기에만 저장되며 공개 여부는 사용자가 나중에 직접 선택한다.
- 계정·게스트 간 초안 소유 범위가 섞이지 않아야 한다.
- `CLASSIFIED_SIGNAL`은 명시적 공개 전 제목·본문·메모·수치·태그·지도 텍스트·접근성 이름을 노출하지 않는다.

### 모바일

- 모바일을 별도 축소 제품으로 만들지 않는다.
- 데스크톱과 같은 IA·데이터 계약·행동 흐름을 유지한다.
- Hero 통계는 모바일 첫 화면에서 숨긴다.
- 고정 제출 액션, 키보드, safe area, 하단 내비, 마지막 콘텐츠의 실제 geometry를 검증한다.

## 4. 2026-08-05 Production 기준선

### 제품·UI

- HomeV2와 `발견 → 기록 → 연결` Hero
- 게스트 기록·공개 경계 안내
- 소스별 부분 장애와 정상 빈 상태 구분
- Network 목록·지도·상세의 일관된 공개 신호 계약
- 모바일 고정 제출 액션과 하단 내비 충돌 방지
- SF 관측 정보 공개 목록과 관리자 편집 흐름
- Profile·Login·Log·Network 반응형 정비

### 데이터·API

- `/api/home-feed` 통합 피드와 소스별 `sourceStatus`
- 영속 archive cache와 제한된 stale fallback
- 서명된 archive sync 및 GitHub Actions 동기화
- `/api/exploration-log` 공개 GET 전용화, 비-GET 405
- SF 관측 정보 스키마·상태 lifecycle·관리자 충돌 처리
- 공개 탐사 로그 RPC의 spoiler 분류 전달
- Supabase 로컬·원격 migration 9/9 일치

### 검증 기준선

- lint: 통과
- build: 통과
- release contracts: 통과
- E2E: 45 passed / 5 skipped
- Screen QA: 32/32
- 핵심 desktop/mobile axe 위반: 0
- Production 주요 route/API: HTTP 200
- 익명 `POST /api/exploration-log`: HTTP 405
- 배포 후보 매니페스트 불일치: 0

## 5. 현재 비차단 P2 백로그

### 보안·데이터

- [ ] 공개 RPC에서 spoiler 원문을 제거하는 transport redaction
- [ ] 관리자 삭제에 `updated_at` 기반 낙관적 동시성 적용
- [ ] migration smoke 식별자를 실행별 고유 값으로 변경

### 운영

- [ ] stale fallback을 `HIT/MISS`와 별도의 `STALE` 상태로 기록
- [ ] 소스별 실패율·응답 시간·stale 사용률 관측
- [ ] Production 오류 및 재시도 추적

### 웹 기본기

- [ ] wildcard 404 경험 추가
- [ ] 비-JS 크롤러용 경로별 canonical·OG 개선
- [ ] 색상 대비 release gate 강화

### 기술 부채·테스트

- [ ] React Router 보안 권고를 반영한 예방적 업데이트 검토
- [ ] E2E skip 5개를 분류하고 가능한 항목을 결정론적 fixture로 전환

## 6. 단계별 로드맵

### Phase 0 — 출시 안정화

목표: 새 구조를 바꾸기 전에 Production의 신뢰성을 측정하고 P2를 축소한다.

- Production 소스 상태와 캐시 fallback 관측
- 익명 쓰기와 spoiler 누출 회귀 감시
- transport redaction 및 관리자 삭제 동시성 보강
- 404·SEO·E2E skip 정리
- 배포 후보와 Git commit·Preview·Production 동일성 유지

통과 기준:

- P0 0 / P1 0
- 익명 공개 쓰기 0건
- spoiler 사전 노출 0건
- 장애를 정상 빈 상태로 표시하는 사례 0건
- 배포 후보와 commit 불일치 0건

### Phase 1 — 첫 기록 전환

목표: Home을 더 복잡하게 만들지 않고 첫 기록까지의 거리를 줄인다.

상태별 주 CTA 후보:

- 게스트·신규: `첫 작품 발견하기`
- 저장된 초안 보유: `작성 중인 기록 이어가기`
- 최근 기록 보유: `최근 탐사 이어가기`
- 공개 신호 보유: `내 신호 확인하기`

복잡한 온보딩 마법사보다 현재 상태에 맞는 하나의 주 행동을 우선한다.

### Phase 2 — 발견 품질

목표: 작품·관측 정보·SF 개념을 실제 데이터로 연결한다.

- 작품 상세의 관련 SF 개념
- 관측 정보에서 관련 작품으로 이동
- 같은 질문을 다룬 작품 연결
- 작품별 공개 감상 신호
- 출처·마지막 갱신 시점
- 탐사 영역의 검색·필터 IA 통합

데이터가 충분하지 않으면 대형 그래프를 만들지 않고 명시적 텍스트 연결부터 시작한다.

### Phase 3 — 공개 커뮤니티

목표: 공개 신호가 충분히 쌓인 후 안전한 연결 기능을 확장한다.

선행 조건:

- transport redaction
- 신고·숨김·관리자 처리
- 계정 삭제 시 공개 기록 정책
- 닉네임·익명성 정책
- 공개·비공개·분류 신호 lifecycle

후보 기능:

- 작품별 감상 신호 모아보기
- 개념별 기록 연결
- 탐사자의 공개 프로필
- 신호 저장·나중에 읽기

댓글·팔로우보다 작품과 감상 사이의 연결을 먼저 강화한다.

### Phase 4 — 실제 데이터 기반 관측소

목표: 충분히 축적된 데이터로 시간·작품·개념 간 변화를 조용히 관측한다.

- 최근 자주 기록되는 SF 개념
- 새 감상 신호가 발생하는 작품
- 시간에 따른 질문과 감정 변화
- 작품이 개념으로 연결되는 방식

데이터가 적은 동안에는 가짜 활동량이나 과장된 네트워크를 표시하지 않는다.

## 7. 측정할 제품 퍼널

초기에는 임의의 성장 목표보다 실제 기준선을 먼저 수집한다.

### 발견

- Home → 탐사 클릭
- 작품 목록 → 상세 진입
- 오늘의 발견 → 관련 작품 진입

### 기록

- 기록 시작
- 초안 저장
- 기록 완성
- 로그인 전 초안 복구
- 중도 이탈 지점

### 공개

- 공개 선택률
- 비공개 유지율
- 분류 신호 선택률
- 공개 후 Network 진입률

### 연결

- Network 방문
- 공개 신호 상세 열람
- 신호 → 작품 이동
- 작품 → 다른 공개 신호 이동

### 안정성

- 소스별 실패율과 응답 시간
- stale fallback 사용률
- 정상 empty와 error 발생 비율
- 모바일 오버플로·고정 UI 회귀

## 8. 역할별 책임

- **보라**: 제품 우선순위, 범위 통제, 최종 검수, 출시 판정
- **윤경**: 보안·개인정보·공개 범위·데이터 무결성
- **현정**: 제품 위계·접근성·모바일·시각 품질
- **리아**: 구현·테스트 계약·회귀·릴리스 자동화
- **선영**: SF 한국어 용어·안내 문구·관측 정보 편집 문체

## 9. 변경 관리 규칙

기능 또는 IA를 변경할 때 다음 순서로 관리한다.

1. 사용자 상태와 제품 문제를 먼저 정의한다.
2. 정상·빈 상태·오류·부분 장애·공개 범위를 명세한다.
3. 테스트 계약을 먼저 추가하거나 함께 갱신한다.
4. 데스크톱과 모바일을 같은 반응형 구조로 검증한다.
5. Preview를 실제 사용자 여정과 긴 스크롤까지 검증한다.
6. 윤경·현정·리아 독립 검토 후 보라가 후보를 판정한다.
7. 승인된 매니페스트와 동일한 후보만 commit·push·Production에 배포한다.
8. 출시 후 결과와 새 결정·P2를 이 문서에 반영한다.

## 10. 다음 우선순위

1. 1~2주 Production 행동·오류 기준선 수집
2. transport redaction과 관리자 삭제 동시성 보강
3. stale cache 운영 라벨과 소스별 관측성 개선
4. E2E skip 5개 및 wildcard 404 정리
5. 게스트·신규·기존 사용자별 Home 주 CTA 설계
6. 작품·개념·관측 정보의 실제 연결 데이터 강화
7. 공개 신호가 충분히 쌓인 뒤 Network 시각화 확장
